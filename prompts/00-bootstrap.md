Ты — участник 3 и интегратор команды. Сейчас выполни только начальный bootstrap, чтобы три Codex могли работать независимо. Не реализуй вместо других участников весь продукт.

Сначала прочитай AGENTS.md, docs/BRIEF.md, docs/CONTRACT.md, src/contracts/index.ts. Проверь git status и текущую структуру. В текущем стартовом пакете есть спецификация, контракты, синтетический каталог и mock-ответы; готового Next.js приложения пока нет. Сохрани эти файлы. Если в репозитории уже есть приложение — используй его, не создавай второе и не уничтожай существующие изменения.

Подготовь одно приложение Next.js App Router + TypeScript + Tailwind с src/ и alias @/*. Все участники используют npm и один package-lock.json. Добавь только необходимое: openai, zod, vitest; для запланированных e2e — @playwright/test. Версии выбери совместимые с окружением, зафиксируй lockfile; не обновляй их в середине работы без причины. Действующие Next.js/SDK API сверяй с официальной документацией, а не выдумывай. Не требуй API-ключ для установки, сборки и тестов.

Если генератор отказывается работать в непустом каталоге, создай каркас во временной соседней папке и перенеси только отсутствующие конфиги/файлы. Не перезаписывай AGENTS.md и контракт. Полезные framework-инструкции генератора сохрани отдельно или аккуратно добавь, не стирая правила проекта.

Подготовь scripts: dev, build, start, lint, typecheck, test, test:e2e. test должен запускать vitest run без watch. Создай минимальные page.tsx, layout.tsx с lang=ru и globals.css. Не подключай загрузку внешних шрифтов как условие успешной сборки. Создай только нужные папки под владение трех участников.

Реализуй src/lib/client-api.ts с пятью async-функциями из CONTRACT.md: getCatalog, simulate, previewEvent, confirmEvent, explain. Они возвращают ApiResponse<T> и умеют реальный fetch к указанным маршрутам либо явный mock-режим NEXT_PUBLIC_USE_MOCK_API=true. Mock использует src/mocks/requests.json и соответствующие response fixtures, сравнивая нормализованные ID. Не отдавай fixture другого плана за ответ на произвольный запрос. Для неподдерживаемой mock-комбинации возвращай явную ошибку MOCK_SCENARIO_NOT_DEFINED. Для explain допускается только соответствующий ему eventConfirm из fixtures; не используй объяснение школы для произвольного базового плана. В интерфейсе mock-режим должен быть явно обозначен.

Создай минимальный тест загрузки каталога/контракта, чтобы test не падал из-за отсутствия тестов. Движок и API сейчас не реализуй: реальные маршруты добавишь на следующем этапе после передачи каркаса. Не создавай заглушки движка с фальшивыми цифрами, которые можно принять за продакшен.

Проверь typecheck, test, build и smoke запуска, насколько позволяет среда. Не утверждай, что проверка прошла, если ее не запускал. После успешного bootstrap сделай commit shared-contract-v1. Предоставь команде его hash и команды запуска. Не создавай/пушь удаленный репозиторий без предоставленного remote и разрешения. Дальше участники 1 и 2 начинают от этого commit; ты продолжаешь по prompts/03-api-ai-integration.md в своей ветке feat/api-ai.

В финальном отчете bootstrap перечисли созданные файлы, экспортируемые функции адаптера, версии зависимостей, команды и результаты проверок, ограничения mock и готовность передать папки другим участникам. Не выводи значения секретов.
