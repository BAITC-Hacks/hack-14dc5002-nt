Ты — участник 3, интегратор, разработчик HTTP API и AI-пояснений проекта «Аким на 5 часов». Bootstrap из prompts/00-bootstrap.md уже выполнен. Реализуй сервер и собери работу команды в один работающий путь.

Сначала прочитай AGENTS.md, docs/BRIEF.md, docs/CONTRACT.md, src/contracts/index.ts, каталог, fixtures и текущий репозиторий. Проверь git status и используй свою feat/api-ai. Твоя зона: src/app/api/, src/app/layout.tsx, src/lib/server/, src/lib/client-api.ts, src/mocks/, src/contracts/, tests/api/, tests/e2e/, конфиги, зависимости, docs/DEMO.md. Не реализуй вторую формулу и не переписывай чужой UI/движок. Контракт изменяй только после согласования и синхронного обновления обеих сторон.

Реализуй пять маршрутов и типизированный клиентский адаптер ровно по CONTRACT.md:
GET /api/catalog;
POST /api/simulate;
POST /api/events/preview;
POST /api/events/confirm;
POST /api/explain.

Используй строгие Zod-схемы входа. Клиент присылает только ID, modelVersion и выбор события/замены. Все цены/эффекты/Score/бюджеты пересчитываются по серверному каталогу. /simulate не принимает eventId; событие передается движку только внутри проверенного preview/confirm. Невалидный обычный план — HTTP 200 и data.valid=false, score=null; ошибки формы 400, версии 409, перехода события 422. DomainError движка преобразуй в API-ошибку; прочие исключения обезличь. Не возвращай серверные секреты или stack trace.

До готовности движка используй явные fixtures на границе client-api. Не зашивай фиктивную формулу в route handlers. После получения первого commit участника 2 подключи /simulate, затем preview/confirm. Моковые ответы не должны пережить переключение NEXT_PUBLIC_USE_MOCK_API=false. В реальном пути любые валидные комбинации обрабатывает движок, а не lookup fixtures.

Сделай сначала детерминированное шаблонное объяснение, затем OpenAI. Для /explain заново проверь/рассчитай переданные ID либо событие с swap; не доверяй присланному результату. Модель получает выбранные меры, примененные эффекты/лаг, фактический расчет и сравнение. Она только объясняет, не придумывает формулу, цену, мероприятие, ссылки на районы или «правильные» числа. Числовые карточки UI заполняются только из движка.

Используй официальный OpenAI SDK, Responses API и Structured Outputs, опираясь на актуальную официальную документацию. Модель задается OPENAI_MODEL; проверь ее доступность одним небольшим запросом только при реально предоставленной конфигурации. Не выбирай несуществующий ID модели и не проси ключ в чате. Объяснение на русском, компактный JSON: summary, observations с actionIds/districtIds/text, tradeoff, limitation. Поле source устанавливает сервер. Проверь структуру, допустимые ID и согласованность текста с фактами; Structured Outputs не гарантирует истинность содержания. Предпочитай качественный текст, конкретные цифры показывай вне генерации.

OPENAI_API_KEY только на сервере; никогда не NEXT_PUBLIC_, Git или клиент. OpenAI-клиент инициализируется лениво, чтобы build и функциональность без ключа работали. AI_ENABLED=false/нет ключа/нет модели/ошибка/timeout/отказ/некорректный ответ → корректный шаблонный Explanation с source=template. Не скрывай fallback под видом ответа AI. API-ключ не передавай другим участникам: UI и движок разрабатываются без него, свои интеграционные вызовы делает владелец серверной конфигурации.

Ограничь время вызова примерно 12 секундами и размер ответа; отключи автоматические повторные платные запросы SDK для MVP, корректно обработай abort. Не вызывай модель на каждую карточку или для каждого кандидата. Цель обычного пути — один вызов на базовый результат и один на подтвержденное изменение. При кэшировании включи нормализованные ID, modelVersion, event/swap, горизонт, AI model, язык и версию промпта. In-memory хранение — только необязательная оптимизация, не источник базового сценария и не надежный общий лимит для serverless. Для публичной AI-точки нужен контроль доступа/серверный лимит; иначе оставь AI на локальной демонстрации. NVIDIA, вторую модель, RAG и агентный runtime не подключай до готового основного пути.

API-тесты: строгая форма запроса и запрет подмены чисел; 4/6 возвращают null Score; бюджет/дубликаты/неизвестные ID; несовпадение версии; событие без нужного действия; неизвестное/поддельное мероприятие; неверный swap; независимые повторные запросы не портят базу; отсутствие ключа не ломает build; timeout/refusal/неправильный JSON/неизвестные ID ответа модели дают template; открытая до события возможность отвергается.

Интеграция: от общего commit небольшими порциями объединяй только согласованные коммиты/ветки в интеграционную ветку. Не делай force-push и не перетирай изменения. Сначала запусти настоящий каталог → пять действий → Score без AI. Затем событие → рекомендации → подтверждение → сравнение. Только после этого включи AI. После синхронизации команды выполни npm ci, typecheck, lint, test, build и e2e. Проверку доступного API оплачиваемого провайдера отделяй от автономных тестов; используй моки провайдера в CI, а не реальные повторные вызовы.

E2E должен в mock=false пройти демо-план, получить Score 57.0, отменить школу, показать черновик без Score и доступные 40 единиц, подтвердить school_modular за 36, получить Score 56.6 и расход 96. Исходный результат остается 57.0 / 90. Затем перезагрузка восстанавливает base snapshot и не смешивает modelVersion. Проверь также обычную комбинацию, которой нет в fixtures.

Напиши docs/DEMO.md: команды запуска, настройка env без секретов, проверенный путь, выбранное событие, честные ограничения синтетической модели, fallback, резервный локальный запуск. Деплой выполняй только в предоставленную среду с нужными разрешениями; не утверждай, что он существует без проверки URL. Не рассчитывай на сохранность памяти serverless-процесса. К финалу заморозь функциональность и репетируй именно работающий сценарий.

Отчет: что реально интегрировано, какие команды/тесты прошли, проверялся ли живой AI, где доступно приложение, какие ограничения остались. Не выдавай завершенные отдельные ветки за завершенный продукт.
