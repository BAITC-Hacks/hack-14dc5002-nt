# Сценарий демонстрации и приемка

## Общая версия для main — 23 сентября 2026

Объединены `Bakytzhan` до `710d106` и интерфейс `Semba` до `c0bf5d6`;
merge-коммит — `5baf8d0`. Код движка `Nikita` до `8a7b6be` уже импортирован
в серверную интеграцию с адаптацией путей. Ветка Nikita отдельно не сливалась.

Повторно проверена объединённая версия: typecheck — PASS; lint — 0 ошибок,
4 предупреждения о неиспользуемых аргументах в старых заглушках движка;
132 Vitest + 77 тестов движка — PASS; 581 проверка fixtures — PASS;
10 браузерных проверок Edge (desktop/mobile) — PASS;
2 HTTP E2E с настоящим API — PASS. Production build с mock UI и без ключа — PASS.
Платных AI-вызовов в этой проверке не выполнялось. GitHub Actions остаётся отключённым.

Это общая проверенная основа для команды. Пока интерфейс использует `demo-v1`
с `NEXT_PUBLIC_USE_MOCK_API=true`, а серверные маршруты работают с `organizer-v1`.
Сембе предстоит подключить `organizer-api`, выбор районов и языка. Публичный сайт
и доступ браузера к платному AI ещё не настроены; `AI_ENABLED=true` само по себе
не открывает этот доступ. Инструкции синхронизации — в README.

Ниже сохранены отчёты предыдущих этапов и их проверки на соответствующих коммитах.

## Текущий серверный этап Bakytzhan — 23 сентября 2026

В Bakytzhan интегрирован движок Nikita `8a7b6be`, добавлены живые HTTP-маршруты
расчёта, preview/confirm двух видов событий и explain на RU/KK/EN.
Пример organizer-v1: стоимость 95, Score 56.54307; при отмене M7 возвращаются 24,
доступны 29, промежуточная четвёрка не имеет Score.

Сервер проверен отдельно от прежнего интерфейса. UI Сембы продолжает работать на
старом mock demo-v1: его миграция на районы/selections/языки ещё предстоит.
Числа двух моделей не взаимозаменяемы. Для нового API используйте
[инструкцию интеграции](ORGANIZER-INTEGRATION.md), а не старый сценарий ниже.

Реальный локальный вызов `gpt-6-luna` вернул HTTP 200 и `source:ai`.
Модель выбирает и упорядочивает проверенные факты; выводимый текст и числа
формируются сервером. Автономные проверки используют mock провайдера.
Локальный `.env.local` сохранён, AI по умолчанию остался выключенным.
Публичного деплоя и объединения с main на этом этапе нет.

### Фактические проверки серверного этапа

Среда: Windows, Node.js 24.11.1, Next.js 15.5.26; зависимости из существующей установки
по package-lock.json, без обновления пакетов. Проверки оплачиваемого провайдера
выполнялись отдельно от автоматических тестов.

| Команда / проверка | Результат |
| --- | --- |
| `npm run typecheck` | PASS |
| `npm run lint` | 0 ошибок, 5 предупреждений: useMemo в UI и неиспользуемые аргументы старых event-заглушек Никиты |
| `npm test` | PASS: 132 Vitest + 77 Node-тестов движка |
| Организаторские fixtures | Все 47 сверены с движком и локализованными шаблонами внутри Vitest |
| `node verification/check-fixtures.mjs` | PASS: 581 проверка прежнего demo-v1 |
| `npm run test:api:e2e` | PASS: 2 сквозных HTTP-теста с mock=false, без ключа; внутри оба события и RU/KK/EN |
| `npm run test:e2e`, Edge, CI=true | PASS: 10 проверок старого mock UI, desktop и мобильная эмуляция |
| `npm run build`, пустые ключ/модель, AI=false, mock=false | PASS; все пять HTTP-маршрутов собраны |
| `next start` без ключа | PASS: каталог, реальный расчёт 56.54307/95, казахский template и страница HTTP 200 |
| OpenAI Responses, настроенная `gpt-6-luna` | PASS: прямой минимальный запрос и локальный `/api/explain`, включая браузерный Origin, вернули проверенные факты/source=ai |

В `.git` отслеживается только `.env.example`; `.env.local` игнорируется.
В проверенных клиентских JS-файлах локальный API-ключ не найден.
Временные серверы проверки остановлены. Остающаяся работа участника 1 — подключение
интерфейса к новому контракту и выбор районов/языка; это ещё не проверка нового UI.

## История: интеграция UI и каркаса Semba

В ветке `Semba` объединены интерфейс участника 1 и каркас участника 3 из `Bakytzhan`
(коммит объединения `958427c`). В исходниках есть выбор мероприятий, загрузка демо,
результаты по городу и районам, события, предварительные замены и сравнение планов.
Базовый план сохраняется в localStorage как ID и версия модели; после перезагрузки
числа запрашиваются заново через адаптер.

На этом историческом этапе путь использовал только mock-сценарии `demo-v1`.
Серверный этап Bakytzhan описан выше; нижеследующие результаты относятся к старой проверке UI.

### Запуск

```sh
npm ci
npm run dev
```

В локальном `.env.local` для первого запуска нужны только публичный переключатель
mock и выключенный AI:

```dotenv
NEXT_PUBLIC_USE_MOCK_API=true
AI_ENABLED=false
```

Если `.env.local` уже существует, сохрани остальные настройки. API-ключ для каркаса
не нужен; этот файл игнорируется Git и не передаётся другим участникам.

Для production-запуска:

```sh
npm run build
npm run start
```

### Проверки текущего объединённого состояния

Проверяется объединённый интерфейс `Semba` с каркасом `Bakytzhan`. Текущие результаты:

| Проверка | Фактический результат |
| --- | --- |
| `npm run typecheck` | PASS: генерация типов маршрутов Next.js и TypeScript |
| `npm run lint` | Exit 0; одно существующее предупреждение `react-hooks/exhaustive-deps` в `SimulationDashboard.tsx:210` |
| `npm test` | PASS: 55 тестов в 5 файлах |
| `node verification/check-fixtures.mjs` | PASS: 581 проверка |
| `npx tsc --noEmit --strict --target ES2022 --module ESNext --moduleResolution bundler verification/fixture-literals.ts src/contracts/index.ts` | PASS |
| `npm run test:e2e` с `PLAYWRIGHT_CHANNEL=msedge`, `CI=true` | PASS: 10 тестов (5 desktop + 5 mobile), один worker, 41,9 с |
| `npm run build` с пустыми `OPENAI_API_KEY`/`OPENAI_MODEL`, `AI_ENABLED=false`, `NEXT_PUBLIC_USE_MOCK_API=true` | PASS; то же предупреждение lint |
| GitHub Actions | Задания не начались из-за блокировки оплаты; workflow впоследствии удалён по решению команды |

Предупреждение lint относится к массиву `actions` в зависимости `useMemo`; исправление
в файле интерфейса нужно согласовать с его владельцем. `typecheck` теперь сначала
выполняет `next typegen`, чтобы генерировать типы маршрутов и в чистом клоне.

E2E выполнены в headless Edge на desktop и эмуляции мобильного размера. Проверены
каталог, загрузка демо, отмена школы, замена на модульные корпуса, сравнение
57 / 90 с 56,6 / 96, шаблонное объяснение подтверждённого события и восстановление
базового плана после перезагрузки. Отдельно проверены отсутствие Score/событий для
четырёх мероприятий и отказ восстановления сохранённого плана другой версии.
Это проверки фиксированного mock-пути; реальный движок и платный AI не вызывались.
После добавления сценариев `typecheck` повторно прошёл, lint завершился с тем же
предупреждением. Реальный мобильный телефон не использовался.

GitHub Actions не смог запустить задания: GitHub вернул сообщение
«The job was not started because your account is locked due to a billing issue».
По решению команды файл `.github/workflows/ci.yml` удалён; автоматических запусков
на push/PR больше нет. Локальные проверки и конфигурация Playwright сохранены.
Старые результаты GitHub относятся к прежнему коммиту, а не к выполненным тестам.

Исторические результаты ниже относятся к каркасу до объединения UI.

### История: проверки каркаса Bakytzhan до объединения UI

Среда: Windows, Node.js 24.11.1, npm 11.6.2. Проверялась установленная версия
Next.js 15.5.26. В этой проверке зависимости заново через `npm ci` не устанавливались.

| Проверка | Фактический результат |
| --- | --- |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS |
| `npm test` | PASS: 55 тестов в 5 файлах |
| `node verification/check-fixtures.mjs` | PASS: 581 проверка fixtures/каталога/математики примеров |
| `npx tsc --noEmit --strict --target ES2022 --module ESNext --moduleResolution bundler verification/fixture-literals.ts src/contracts/index.ts` | PASS |
| `npm run test:e2e` с `PLAYWRIGHT_CHANNEL=msedge` | PASS: 4 проверки в headless Edge, desktop и эмуляция Pixel 7 |
| `npm run build` с пустыми `OPENAI_API_KEY`/`OPENAI_MODEL` и `AI_ENABLED=false` | PASS |
| Production-сервер Next.js (`next start`) | `/` и `/api/catalog` вернули HTTP 200 |

Тогда браузерные тесты проверяли заголовок, подключение стилей, пометку mock, отсутствие
горизонтального переполнения и ошибок JavaScript, JSON-envelope каталога. Скриншоты
сохраняются в игнорируемой папке `test-results/`. Это не проверки полного пути выбора
мероприятий/событий. Мобильная проверка выполнялась в эмуляции, не на реальном телефоне.

Команда E2E запускает отдельный dev-сервер на `127.0.0.1:3317` с пустым API-ключом и
выключенным AI. Этот порт должен быть свободен. Для установленного Microsoft Edge
в PowerShell:

```powershell
$env:PLAYWRIGHT_CHANNEL = 'msedge'
npm run test:e2e
```

По умолчанию используется Chromium из Playwright. Для него сначала выполни
`npx playwright install chromium`, затем `npm run test:e2e`. Не запускай `build`
одновременно с E2E: обе команды используют `.next`.

### Исправления по результатам проверки

- Playwright теперь ищет только `tests/e2e`, а Vitest исключает эту папку.
  Раньше `test:e2e` подхватывал Vitest-тесты и падал.
- Mock-адаптер возвращает независимые копии fixtures. Изменение полученного ответа
  не портит следующий вызов; это покрыто регрессионными тестами.
- Дополнены проверки всех пяти функций адаптера, запросов fetch, ошибок и строгих схем.

### Исторические ограничения интеграции Semba до серверного этапа

При исторической HTTP-проверке `POST /api/simulate`, `/api/events/preview`, `/api/events/confirm`
и `/api/explain` вернули **404**: этих маршрутов всё ещё нет в исходниках. Серверные Zod-схемы и
JSON-parser уже есть, но будут подключены к POST-маршрутам на этапе интеграции.

UI уже включён; адаптер поддерживает только запросы из `src/mocks/requests.json`.
Неизвестный план или неподготовленная замена возвращают `MOCK_SCENARIO_NOT_DEFINED`.
Числа приходят из fixtures, а не рассчитываются универсальным движком. Кнопка
объяснения базового плана также возвращает эту ошибку: такого mock пока нет.
Шаблонное mock-объяснение доступно для подтверждённой замены школы на модульные корпуса.

OpenAI, шаблонные серверные объяснения и переключатель RU/KK/EN ещё не реализованы.
Автономные проверки не делают live-запросов генерации. API-ключ для UI не нужен.

При объединении сохранён интерфейс Сембы в `page.tsx`, `globals.css` и `src/features/`.
До подключения Никиты согласовать переход от `demo-v1/actionIds` к его контракту
`organizer-v1/selections` и наличие/форму событий. Ключи не передавать.

## Сценарий mock-демонстрации demo-v1

Ниже описан подготовленный путь UI с fixtures по исходному контракту `demo-v1`.
Используйте именно перечисленные план и замену: произвольные запросы mock-адаптер
не рассчитывает. Это не приёмка с mock=false. После согласования контракта Никиты
сценарий и ожидаемые числа потребуется обновить совместно с командой.

## Основной проход

Открыть «Аким на 5 часов». Видны 100 условных единиц, правило пяти решений, синтетические данные и горизонт 12 месяцев. Нажать «Загрузить демо-план» либо выбрать bus_lanes, school_new, park_local, lighting_smart, services_online, затем «Рассчитать результат». Проверить пять действий, расход 90 и результат fixtures 57.0 (UI может показывать 57 без десятичной части и использовать десятичную запятую).

Зафиксировать исходный результат. Открыть событие «Строительство школы отменено: участок недоступен». В ветке четыре мероприятия, расход 60, доступно 40, официального результата ветки нет. Исходный снимок 57.0 / 90 остается на экране.

Из предложенных вариантов выбрать именно «Модульные учебные корпуса» (school_modular), а не автоматически первый вариант. Они стоят 36, то есть дороже отмененной школы за 30, но укладываются в 40 свободных единиц. Предварительный результат относится к пятерке после замены, не к четверке черновика.

Подтвердить. Получить пять мероприятий, расход 96, новый Score 56.6, изменение -0.4. Показать изменения по районам и объяснение компромисса. Не изображать падение как искусственное «улучшение»: задача — показать последствия и возможность адаптации, а не гарантировать рост Score.

В этом каталоге автобусный парк дает практически тот же городской Score, но другой районный/отраслевой результат; при равном Score он ранжируется выше из-за меньшей стоимости. Поэтому выбираем конкретную замену по названию, а не «первую карточку». Это позволяет обсудить разницу между общим баллом и социальными приоритетами.

## Текст для защиты после интеграции настоящего движка

До серверной интеграции обязательно предваряйте показ словами: «Сейчас демонстрируем
интерфейс на подготовленных тестовых сценариях; движок и LLM подключаются отдельно».

«У вас одинаковый бюджет и ровно пять решений. Результат рассчитывает открытая формула; AI объясняет последствия, а не придумывает баллы. После сохранения исходного плана участок под школу стал недоступен. Мы не удалили исходный результат и не оценили некорректный план из четырех решений: предлагаем проверенные замены, учитываем оставшийся бюджет и сравниваем две валидные пятерки».

## Приемка перед финальным показом с реальным API

Проверьте реальный путь с mock=false; сборку без API-ключа; пять действий в бюджете; 4/6 без Score; превышение 100 без Score; недоступную отмененную школу; зависимость safety_sensors от lighting_smart; отсутствие мутации исходного результата; обработку пустых замен; перезагрузку снимка; timeout AI с пометкой шаблонного ответа. Числа должны совпадать с golden fixtures неизмененной demo-v1.

Публичная копия не должна иметь незащищенный платный AI endpoint. При отсутствии доступа к хостингу/защиты показывайте локальный запуск либо отключите AI в публичной копии. Подготовьте локальную резервную копию и честно подписанный template-режим. Не выдавайте отсутствие живого AI за полноценную интеграцию.

Интегратор дополняет этот файл реально проверенными командами запуска, env-настройками без ключей, результатами e2e и фактическим адресом развертывания при его наличии. Перед защитой сверить требования с полным заданием организатора, которого в этом пакете нет.
